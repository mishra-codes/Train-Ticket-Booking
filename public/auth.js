const api = async (url, options = {}) => { const response = await fetch(url, { headers: { 'Content-Type': 'application/json' }, ...options }); const data = await response.json(); if (!response.ok) throw new Error(data.message || 'Request failed.'); return data; };
const message = document.getElementById('message');
const form = document.getElementById('signinForm') || document.getElementById('signupForm');
form.onsubmit = async event => { event.preventDefault(); try { const data = Object.fromEntries(new FormData(form)); if (form.id === 'signupForm') { const result = await api('/api/signup', { method: 'POST', body: JSON.stringify(data) }); message.textContent = result.message; message.className = 'hint success'; form.reset(); } else { const result = await api('/api/signin', { method: 'POST', body: JSON.stringify(data) }); localStorage.setItem('railReserveUser', JSON.stringify(result)); window.location.href = 'index.html'; } } catch (error) { message.textContent = error.message; message.className = 'hint error'; } };
